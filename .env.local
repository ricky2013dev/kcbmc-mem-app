# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/family_management

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Server Configuration
PORT=5000
NODE_ENV=development

# PostgreSQL Direct Connection (for Drizzle if needed)
PGHOST=localhost
PGPORT=5432
PGUSER=username
PGPASSWORD=password
PGDATABASE=family_management

# Optional: Object Storage (if you plan to add file uploads later)
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=your-bucket-id
# PRIVATE_OBJECT_DIR=/your-bucket/private
# PUBLIC_OBJECT_SEARCH_PATHS=/your-bucket/public

# Replit-specific (only needed when running on Replit)
# REPL_ID=your-repl-id
# REPLIT_DOMAINS=your-domain.replit.dev